import { ParsePipedStringPipe } from './parse-piped-string.pipe';

describe('ParsePipedStringPipe', () => {
  it('should be defined', () => {
    expect(new ParsePipedStringPipe()).toBeDefined();
  });
});
